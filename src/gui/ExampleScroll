		/*
		Search search = new Search();
		
		ArrayList<String> keys = search.getKeysFromSearch(search.perfectProcureView(auth));
		JList<String> keyList = ArrayListToJList.asString(keys);
		keyList.setSelectionMode(ListSelectionModel.SINGLE_INTERVAL_SELECTION);
		keyList.setLayoutOrientation(JList.VERTICAL);
		keyList.setVisibleRowCount(-1);
		
		JScrollPane keyScroller = new JScrollPane(keyList);
		 
		int keyScrollerOffset = 10;
		keyScroller.setPreferredSize(new Dimension(50, h - 50 - keyScrollerOffset));
		keyScroller.setBounds(new Rectangle(keyScrollerOffset, keyScrollerOffset, 50, h - 50 - keyScrollerOffset));
		
		DefaultListModel<String> listModel = new DefaultListModel<String>();
		for(int i = 0; i < keys.size(); i++) {
			listModel.addElement(Issue.stringIssue(auth, keys.get(i)));
		}
		JList<String> issueList = new JList<String>(listModel);
		issueList.setSelectionMode(ListSelectionModel.SINGLE_INTERVAL_SELECTION);
		issueList.setLayoutOrientation(JList.VERTICAL);
		issueList.setVisibleRowCount(-1);
		
		JScrollPane issueScroller = new JScrollPane(issueList);
		issueScroller.setPreferredSize(new Dimension(660, h - 60));
		issueScroller.setBounds(new Rectangle(70, 10, 660, h - 60));
		
		mainFrame.getContentPane().add(keyScroller);
		mainFrame.getContentPane().add(issueScroller);
		*/